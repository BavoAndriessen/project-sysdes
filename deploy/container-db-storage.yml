kind: PersistentVolume
apiVersion: v1
metadata:
  name: containerdb-pv-volume
  labels:
    app: containerdb
spec:
  storageClassName: manual
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/data"
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: containerdb-pv-claim
  labels:
    app: containerdb
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi

